<li>
  <%= gravatar_for user, size: Settings.user.avatar_size.small %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.current_user?(current_user) %>
    | <%= link_to t("delete"), user, method: :delete,
        data: {confirm: t("confirm_destroy")} %>
  <% end %>
</li>
